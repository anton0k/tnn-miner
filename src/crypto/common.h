#pragma once
/* Wave38 full-binary probe compatibility header.
 * The imported VerusHash source includes Bitcoin Core's crypto/common.h, but
 * the current VerusHash object path does not require symbols from it.
 */
